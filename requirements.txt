# Core runtime
Flask>=2.2,<3
flask-cors>=4.0,<7
pydantic>=2.0,<3

# Data
pandas
matplotlib
scikit-learn

# Elasticsearch client (8.x compatible)
elasticsearch>=8.0.0,<9

# Optional: langchain if you plan to run end-to-end chains
langchain>=0.0.300

# LLM / OpenAI (LangChain + native client)
langchain-openai>=0.1.0
openai>=1.0.0

# LLM / Gemini (LangChain + native client) - Keep for now or remove
# langchain-google-genai>=0.0.10
# google-generativeai>=0.6.0

# Utilities / dev
python-dotenv>=1.0.0
pytest>=7.0.0
